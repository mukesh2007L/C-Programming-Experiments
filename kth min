#include <stdio.h>

int main()
{
   int n,i,j,k;
   scanf("%d",&n);
   int arr[n];
   for(i=0;i<n;i++)
   scanf("%d",&arr[i]);
   printf("enter k");
   scanf("%d",&k);
   for(i=0;i<n;i++)
   {
       for(j=i+1;j<n;j++)
       {
           if(arr[i]>arr[j])
          {
           int temp=arr[i];
           arr[i]=arr[j];
           arr[j]=temp;
          } 
       }
   }
     if(k<=n && k>0)
      printf("the %dth min no is: %d",k,arr[k-1]);
    else
      printf("invalid value ofk\n");
    
    return 0;
}
