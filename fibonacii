#include <stdio.h>

int main()
{ 
  int n,f1=0,f2=1,sum,i;
  scanf("%d",&n);
  printf("%d %d",f1,f2);
  for(i=3;i<=n;i++)
  {
      sum=f1+f2;
      printf(" %d",sum);
      f1=f2;
      f2=sum;
  }

    return 0;
}
