#include <stdio.h>

int main()
{
    int n,i;
    scanf("%d",&n);
    int arr[n];
    printf("enter array 1:");
    for(i=0;i<n;i++)
    scanf("%d",&arr[i]);
    int brr[n];
    printf("enter array 2:");
    for(i=0;i<n;i++)
    scanf("%d",&brr[i]);
    int crr[n];
     for(i=0;i<n;i++)
        crr[i]=arr[i]+brr[i];
    printf("sum of array is");
    for(i=0;i<n;i++)
    printf("%d",crr[i]);
       
  
    
   

    return 0;
}
