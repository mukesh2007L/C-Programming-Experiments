# include <stdio.h>
# include <string.h>
int main()
{
    int count=0,length;
    char a[]="I am an indian";
    length=strlen(a);
    int i=0;
    while(i<length)
    {
    if((a[i]!=32) && (a[i+1]==32))
     count++;
     i++;
    }
    if(a[length-1]!=32)
    count++;
    printf("%d",count);
    
    

    return 0;
}
