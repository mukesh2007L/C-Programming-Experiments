
#include <stdio.h>

int main()
{
    int n,i,sum=0;
    scanf("%d",&n);
   for(i=1;i<=n;i++) //1 2 3 4 5
   {
       if(n%i==0)
      {
          printf(" %d",i);
          sum=sum+i;
      }  
   }
   printf("\n%d",sum);

    return 0;
}
