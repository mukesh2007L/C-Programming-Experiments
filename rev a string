#include <stdio.h>
# include <string.h>
int main()
{
    int i=0,j,length,temp;
    char a[]= "mukesh";
    length=strlen(a);
    j=length-1;
    while(i<j)
    {
    temp=a[i];
    a[i]=a[j];
    a[j]=temp;
    i++;
    j--;
    }
    printf("%s",a);

    return 0;
}
